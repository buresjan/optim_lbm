# To create a virtual environment, run the following command in your terminal:
# $ python -m venv optim_lbm
# To activate the virtual environment, use:
# $ source optim_lbm/bin/activate
# To install these requirements, use:
# $ pip install -r requirements.txt
numpy==1.26.0
pandas==2.1.2
matplotlib==3.8.1
trimesh==4.0.2
pyvista==0.42.3
mayavi==4.8.1
scipy==1.11.3
gmsh==4.11.1
black[d]
shapely
networkx
rtree
mapbox_earcut
tqdm